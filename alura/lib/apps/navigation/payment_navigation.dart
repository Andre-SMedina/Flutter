import 'package:flutter/material.dart';

class PaymentNavigation extends StatelessWidget {
  const PaymentNavigation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Payment Navigation'),
      ),
    );
  }
}
