import 'package:flutter/material.dart';

class SingupNavigation extends StatelessWidget {
  const SingupNavigation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('SingUp Navigation'),
      ),
    );
  }
}
