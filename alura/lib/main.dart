import 'package:alura/apps/bank/init_bank.dart';
import 'package:flutter/material.dart';

void main() {
  runApp(const InitBank());
  // runApp(const MaterialApp(home: restaurante.Home()));
  // initialRoute: 'home',
  // onGenerateRoute: RouteGenerator.generateRoute,
}
